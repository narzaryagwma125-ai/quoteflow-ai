# Backend base URL used by Next.js rewrites in production builds.
BACKEND_URL=http://localhost:8000
# Public base URL of the frontend (used for share links back to the app).
NEXT_PUBLIC_APP_URL=http://localhost:3000